<UserControl x:Class="sainim.WPF.Views.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:sainim.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Menu Grid.Row="0" Grid.ColumnSpan="3" Background="WhiteSmoke">
        <MenuItem Header="{DynamicResource File}">
            <MenuItem Header="{DynamicResource Import}" Command="{Binding ImportPsdImage}" />
            <MenuItem Header="{DynamicResource Export}"/>
        </MenuItem>
        <MenuItem Header="{DynamicResource Settings}">
            <MenuItem Header="{DynamicResource Language}">
                <MenuItem Header="English" Command="{Binding ChangeLanguageToEnglish}"/>
                <MenuItem Header="Polski" Command="{Binding ChangeLanguageToPolish}"/>
            </MenuItem>
        </MenuItem>
        <MenuItem Header="{DynamicResource Info}">
            <MenuItem Header="{DynamicResource Credits}"/>
        </MenuItem>
    </Menu>
</UserControl>
